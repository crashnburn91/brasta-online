-- Public profile avatars are uploaded only through the authenticated Brasta API.
-- Unique object names make avatar replacement cache-safe while public reads keep
-- player cards, profiles, chat, and friend lists inexpensive to render.

insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
values (
  'avatars',
  'avatars',
  true,
  1048576,
  array['image/webp']::text[]
)
on conflict (id) do update set
  name = excluded.name,
  public = excluded.public,
  file_size_limit = excluded.file_size_limit,
  allowed_mime_types = excluded.allowed_mime_types;
